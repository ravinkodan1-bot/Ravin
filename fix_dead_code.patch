--- code.gs
+++ code.gs
@@ -118,29 +118,6 @@
       let txn = {};
       headers.forEach((h, i) => txn[h] = row[i]);

-      const itemKeyBase = `${txn.ItemName}|${txn.Brand}`;
-      const qty = parseFloat(txn.Qty) || 0;
-
-      // Initialize helper
-      const getInvKey = (type, loc) => `${itemKeyBase}|${type}|${loc}`;
-      const addInv = (type, loc, amt, isPending = false) => {
-        if(!type || !loc) return;
-        let key = getInvKey(type, loc);
-        if(!inventory[key]) inventory[key] = { physical: 0, pending: 0, saleable: 0, state: godownStates[loc] || 'N/A' };
-        if(isPending) {
-           inventory[key].pending += amt;
-           inventory[key].saleable -= amt; // Lock saleable
-        } else {
-           inventory[key].physical += amt;
-           inventory[key].saleable += amt;
-        }
-      };
-
-      if (txn.Type === "Opening" || txn.Type === "Purchase") {
-        addInv(txn.DestType, txn.DestLocation, qty);
-      }
-      else if (txn.Type === "Transfer") {
-        addInv(txn.SourceType, txn.SourceLocation, -qty);
-        addInv(txn.DestType, txn.DestLocation, qty);
-      }
-      else if (txn.Type === "SaleOrder") {
-        // Dedect from saleable, add to pending, physical remains same until dispatch
-        addInv(txn.SourceType, txn.SourceLocation, qty, true);
-      }
-      else if (txn.Type === "Dispatch") {
-        // This is handled by updating the original SaleOrder status,
-        // but if logged as separate row:
-        // Physical drops, pending drops.
-      }
-    });
-
-    // Handle Dispatch updates by checking statuses of SaleOrders
-    // Actually, in the above logic, we process rows.
-    // Let's refine based on status.
-    inventory = {};
-    data.forEach(row => {
-      let txn = {};
-      headers.forEach((h, i) => txn[h] = row[i]);
-
-      const itemKeyBase = `${txn.ItemName}|${txn.Brand}`;
-      const qty = parseFloat(txn.Qty) || 0;
+      const itemKeyBase = `${txn.ItemName}|${txn.Brand}`;
+      const qty = parseFloat(txn.Qty) || 0;
